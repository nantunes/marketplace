{
  "name": "@pentaho/marketplace",
  "version": "${project.version}",
  "description": "${project.description}",
  "paths": {
    "pentaho/marketplace": "/"
  },
  "packages": [
    "pentaho/marketplace"
  ],
  "map": {
    "pentaho/marketplace": {
      "marketplaceApp": "pentaho/marketplace/js/app",
      "marketplace-lib": "pentaho/marketplace/lib/marketplace"
    }
  },
  "dependencies": {
    "underscore": "${underscore.version}",
    "jquery": "${jquery.version}",
    "angular": "${angular.version}",
    "angular-route": "${angular-route.version}",
    "angular-sanitize": "${angular-sanitize.version}",
    "angular-animate": "${angular-animate.version}",
    "angular-translate": "${angular-translate.version}",
    "angular-bootstrap": "${angular-ui-bootstrap.version}"
  }
}
